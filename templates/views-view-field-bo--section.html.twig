{{ help }}

<section id="collection_{{ bo.id }}" class="parallax-window" data-parallax="scroll" data-image-src="{{ bo.field_image.raw_value.optimized_url }}">
    {{ collection }}
</section>

{# attach_library('bo/bo_parallax') #}