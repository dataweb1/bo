{{ help }}

<figure class="image-container">
{% if bo.display_id == "grid" %}
    <img src="{{ commentStripper(bo.field_image.rendered.view_square) }}" alt="{{ bo.field_image.raw.alt }}" />
{% else %}
    <img src="{{ bo.field_image.raw.original_url }}" style="width: 100%" />
{% endif %}
  {% if bo.field_image.raw.alt != ""  %}
  <figcaption>{{ bo.field_image.raw.alt }}</figcaption>
  {% endif %}
</figure>

{{ attach_library('bo/bo_bundle_image') }}
